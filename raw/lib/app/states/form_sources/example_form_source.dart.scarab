import 'package:get/get.dart';
import 'package:<<projectName>>/app/states/form_validators/<<validatorFileName>>';

class <<className>> {
  late <<validatorName>> validator;

  void init() {
    validator = <<validatorName>>(this);
  }

  Map<String, dynamic> toJson() => {};
}
